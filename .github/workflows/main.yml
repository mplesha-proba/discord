on:
  workflow_dispatch:

jobs:
  discord_message:
    name: Discord message
    runs-on: ubuntu-latest
    steps:
      - name: send message
        run: |
           curl -H "Content-Type: application/json" -X POST -d "{\"content\": \"Посилання на ретроспективу https://softserveinc.zoom.us/j/92510557555?pwd=Rm96dEFsQjNEeFp4bWE2Z0dsSkg4QT09&from=addon\"}" https://discord.com/api/webhooks/${{ secrets.DISCORD_ID }}/${{ secrets.DISCORD_TOKEN }}
