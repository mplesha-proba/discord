on:
  schedule:
    - cron: '52 4 3 1 *'
  workflow_dispatch:

jobs:
  discord_message:
    name: Discord message
    runs-on: ubuntu-latest
    steps:
      - name: send message
        run: |
           curl -H "Content-Type: application/json" -X POST -d "{\"content\": \"З Новим Роком всіх учасників марафону 🙂! У зв'язку з вихідним, чергову ретроспективу проведемо завтра!\"}" https://discord.com/api/webhooks/${{ secrets.DISCORD_ID }}/${{ secrets.DISCORD_TOKEN }}
