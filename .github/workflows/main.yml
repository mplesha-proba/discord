on:
  schedule:
    - cron: '53 4 27 12 *'
  workflow_dispatch:

jobs:
  discord_message:
    name: Discord message
    runs-on: ubuntu-latest
    steps:
      - name: send message
        run: |
           curl -H "Content-Type: application/json" -X POST -d "{\"content\": \"Привіт марафонці, оскільки сьогодні вихідний 🙂, ретроспектива 9-го спрінта відбудеться завтра, 28.12-го, о 13:00\"}" https://discord.com/api/webhooks/${{ secrets.DISCORD_ID }}/${{ secrets.DISCORD_TOKEN }}
