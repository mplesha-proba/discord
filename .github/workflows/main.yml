on:
  schedule:
    - cron: '53 5 5 5 *'
  workflow_dispatch:

jobs:
  discord_message:
    name: Discord message
    runs-on: ubuntu-latest
    steps:
      - name: send message
        run: |
           curl -H "Content-Type: application/json" -X POST -d "{\"content\": \"Доброго дня марафонці! Сьогодні о 13:00 ми проведемо чергову ретроспективу 🙂! Перед ретроспективою - зустріч з представниками сертифікаційного центру. \"}" https://discord.com/api/webhooks/${{ secrets.DISCORD_ID }}/${{ secrets.DISCORD_TOKEN }}
